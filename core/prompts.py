"""Промпты для LLM по режимам обработки."""
from __future__ import annotations

from typing import Dict

SYSTEM_PROMPT = """Ты опытный редактор и аналитик. Тебе дают текст чьего-то выступления, лекции, \
разговора или заметки, надиктованной голосом. Твоя работа — изложить СОДЕРЖАНИЕ: мысли, факты, \
аргументы, решения.

Главные правила:
1. Пиши о предмете, а не о тексте. Никогда не упоминай «транскрибацию», «аудио», «запись», \
«файл», «фрагмент», «говорящего», «спикера», «в тексте сказано». Излагай мысли напрямую, \
как в хорошем конспекте: не «спикер считает, что X», а «X». Автора или участника называй, \
только если это важно для смысла (например, кто взял задачу).
2. Объём соразмерен исходнику. Одна-две фразы на входе дают несколько строк на выходе, \
а не страницу. Никогда не растягивай текст и не повторяй одно и то же разными словами.
3. Только то, что есть в тексте. Не додумывай, не дополняй общими знаниями, не угадывай \
продолжение, не давай оценок, морали и комментариев о манере речи или лексике.
4. Раздел, для которого в тексте нет материала, просто пропусти. Не пиши «не упоминается», \
«информации нет».
5. Сохраняй точные цифры, имена, названия и термины. Очевидные ошибки распознавания речи \
исправляй молча.
6. Пиши на русском языке, простым и ясным языком.

Формат: Markdown, без эмодзи, без обёртки в код-блок. Первая строка — заголовок уровня # \
с темой по сути содержания (3–8 слов), без слов «конспект», «резюме», «выводы»."""

MODE_PROMPTS: Dict[str, Dict[str, str]] = {
    "insights": {
        "title": "Ключевые выводы",
        "user": """Выдели ключевые выводы из текста ниже (в нём около {words} слов).

Структура (включай только разделы, для которых есть материал):
# Тема

## Главные мысли
Нумерованный список, от самого важного. Каждый пункт — законченная мысль в одну-две фразы, \
с конкретикой из текста. Для короткого текста хватит 1–3 пунктов, для длинного не больше 7.

## Факты и цифры
Числа, даты, суммы, имена, названия — списком.

## Что из этого следует
Рекомендации, решения или выводы, которые прямо прозвучали.

## Итог
Одно-два предложения о самом главном.

Текст:
---
{text}
---

Ответь на русском в Markdown, без обёртки в код-блок.""",
        "map": """Это часть длинного текста. Выпиши из неё коротко, без вступлений:
- главные мысли (каждая одной фразой, с конкретикой);
- факты, цифры, имена, названия;
- прозвучавшие рекомендации и решения.
Пропускай пункты, для которых нет материала. Не упоминай, что это часть текста или запись.

Часть текста:
---
{text}
---

Ответь на русском в Markdown, без обёртки в код-блок.""",
        "reduce": """Ниже заметки по частям одного длинного текста. Объедини их в единые ключевые выводы: \
убери повторы, сгруппируй близкое, расставь по важности.

Структура (включай только разделы, для которых есть материал):
# Тема
## Главные мысли (нумерованный список, не больше 7)
## Факты и цифры
## Что из этого следует
## Итог (одно-два предложения)

Заметки:
---
{fragments}
---

Ответь на русском в Markdown, без обёртки в код-блок.""",
    },

    "lecture": {
        "title": "Конспект лекции",
        "user": """Составь конспект по тексту ниже (в нём около {words} слов), такой, по которому \
можно повторить материал, не перечитывая исходник.

Структура:
# Тема

Затем разделы уровня ## с названиями по смыслу (не «Введение» и «Основная часть»), \
в порядке изложения. Внутри раздела — тезисы списком: определения терминов, \
объяснения, примеры, цифры. Самые точные или яркие формулировки можно дать цитатой (> …) \
дословно.

## Главное
Три–пять пунктов, которые нужно запомнить.

Для короткого текста не дроби на разделы: заголовок и несколько тезисов.

Текст:
---
{text}
---

Ответь на русском в Markdown, без обёртки в код-блок.""",
        "map": """Это часть длинной лекции или выступления. Законспектируй её: смысловые блоки \
с короткими заголовками, под ними тезисы, определения, примеры и цифры. Без вступлений \
и без упоминания, что это часть текста или запись.

Часть текста:
---
{text}
---

Ответь на русском в Markdown, без обёртки в код-блок.""",
        "reduce": """Ниже конспекты частей одной лекции по порядку. Объедини их в единый конспект: \
сохрани порядок изложения, убери повторы, объедини близкие блоки.

Структура:
# Тема
## Разделы с названиями по смыслу (тезисы, определения, примеры, цифры)
## Главное (три–пять пунктов)

Конспекты частей:
---
{fragments}
---

Ответь на русском в Markdown, без обёртки в код-блок.""",
    },

    "summary": {
        "title": "Краткое резюме",
        "user": """Напиши краткое резюме текста ниже (в нём около {words} слов). Резюме должно быть \
заметно короче исходника: для текста в пару предложений хватит одного-двух предложений.

Структура:
# Тема

Суть в одном абзаце (2–4 предложения; для короткого текста — одно-два).

## Ключевые моменты
Три–пять пунктов, только если текст достаточно длинный.

Текст:
---
{text}
---

Ответь на русском в Markdown, без обёртки в код-блок.""",
        "map": """Это часть длинного текста. Перескажи её суть в 2–4 предложениях: главные мысли, \
цифры и факты. Без вступлений и без упоминания, что это часть текста или запись.

Часть текста:
---
{text}
---

Ответь на русском, без обёртки в код-блок.""",
        "reduce": """Ниже пересказы частей одного длинного текста по порядку. Напиши по ним единое \
краткое резюме без повторов.

Структура:
# Тема
Суть в одном абзаце (3–5 предложений).
## Ключевые моменты (три–пять пунктов)

Пересказы частей:
---
{fragments}
---

Ответь на русском в Markdown, без обёртки в код-блок.""",
    },

    "action_plan": {
        "title": "План действий",
        "user": """Составь план действий по тексту ниже (в нём около {words} слов): что нужно сделать, \
исходя из прозвучавших задач, решений, договорённостей и рекомендаций.

Структура:
# Тема

## Задачи
Чекбоксы «- [ ] …», сгруппированные по смыслу или приоритету, если задач много. Каждая задача \
начинается с глагола и понятна без исходника. Ответственного и срок указывай в скобках, \
только если они названы в тексте; сроки и исполнителей не придумывай.

## Открытые вопросы
Что осталось нерешённым или требует уточнения, только если это прозвучало.

Если в тексте нет ни задач, ни решений, ни рекомендаций, после заголовка напиши одной строкой: \
«Конкретных действий из этого текста не следует.» и больше ничего.

Текст:
---
{text}
---

Ответь на русском в Markdown, без обёртки в код-блок.""",
        "map": """Это часть длинного текста. Выпиши из неё все задачи, решения, договорённости \
и рекомендации: что сделать, кто (если назван), к какому сроку (если назван). Ничего не \
придумывай. Если ничего такого нет, ответь одним словом: «нет».

Часть текста:
---
{text}
---

Ответь на русском, без обёртки в код-блок.""",
        "reduce": """Ниже задачи, выписанные из частей одного длинного текста. Составь из них единый \
план действий: убери повторы, сгруппируй по смыслу или приоритету.

Структура:
# Тема
## Задачи (чекбоксы «- [ ] …»; ответственный и срок в скобках, только если названы)
## Открытые вопросы (только если есть)

Если задач нет, после заголовка напиши: «Конкретных действий из этого текста не следует.»

Задачи из частей:
---
{fragments}
---

Ответь на русском в Markdown, без обёртки в код-блок.""",
    },
}


def get_system_prompt() -> str:
    return SYSTEM_PROMPT


def get_user_prompt(mode: str, text: str) -> str:
    """Получить пользовательский промпт для режима."""
    if mode not in MODE_PROMPTS:
        raise ValueError(f"Неизвестный режим: {mode}. Доступные: {list(MODE_PROMPTS.keys())}")
    return MODE_PROMPTS[mode]["user"].format(text=text, words=len(text.split()))


def get_map_prompt(mode: str, text: str) -> str:
    """Получить map-промпт для обработки чанка."""
    if mode not in MODE_PROMPTS:
        raise ValueError(f"Неизвестный режим: {mode}")
    return MODE_PROMPTS[mode]["map"].format(text=text)


def get_reduce_prompt(mode: str, fragments: str) -> str:
    """Получить reduce-промпт для объединения результатов."""
    if mode not in MODE_PROMPTS:
        raise ValueError(f"Неизвестный режим: {mode}")
    return MODE_PROMPTS[mode]["reduce"].format(fragments=fragments)


def get_mode_title(mode: str) -> str:
    """Получить название режима."""
    if mode in MODE_PROMPTS:
        return MODE_PROMPTS[mode]["title"]
    return mode
